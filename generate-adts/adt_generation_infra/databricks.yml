# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "adt_generation_infra"

variables:
  catalog:
    description: "Unity Catalog catalog name for volumes"
    default: "main"
  
  schema:
    description: "Unity Catalog schema name for volumes"
    default: "default"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://fe-sandbox-himss2026.cloud.databricks.com
    variables:
      catalog: "dev_catalog"
      schema: "dev_schema"

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://fe-vm-mkgs-databricks-demos.cloud.databricks.com
      root_path: /Workspace/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as matthew.giglia@databricks.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: matthew.giglia@databricks.com
    variables:
      catalog: "himss"
      schema: "redox"

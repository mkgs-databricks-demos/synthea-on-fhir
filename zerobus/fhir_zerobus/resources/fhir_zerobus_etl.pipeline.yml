# The main pipeline for fhir_zerobus

resources:
  pipelines:
    fhir_zerobus_etl:
      name: fhir_zerobus_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/fhir_zerobus_etl"

      libraries:
        - glob:
            include: ../src/fhir_zerobus_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}

resources:
  schemas:
    epic_on_fhir_schema:
      name: ${var.schema}
      grants:
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.catalog}
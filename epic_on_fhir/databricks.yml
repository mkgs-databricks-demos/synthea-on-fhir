# This is a Databricks asset bundle definition for epic_on_fhir.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: epic_on_fhir
  uuid: 6fff4968-d24d-4d81-9a83-d4d2e5a38493

include:
  # - resources/*.yml
  - resources/epic*.yml
  - resources/*app.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  secret_scope_name: 
    description: The secret scope name to use for the Epic on FHIR credentials
    default: epic_on_fhir_oauth_keys
  epic_kid: 
    description: The EPIC kid Used to Connect with JWT OAuth2
  run_as_user:
    description: The User that the workflows should run as.
  pipeline_run_as_user:
    description: The user or principal that the Spark Declarative Pipelines should run as.  
    default: ${var.run_as_user}
  algo:
    description: Encryption algorithm used. 
    default: RS384

  

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://fe-vm-mkgs-databricks-demos.cloud.databricks.com
    variables:
      catalog: mkgs_dev
      schema: epic_on_fhir
      epic_kid: fy27fde
      run_as_user: matthew.giglia@databricks.com


  prod:
    mode: production
    workspace:
      host: https://fe-vm-mkgs-databricks-demos.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/matthew.giglia@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/matthew.giglia@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: mkgs_dev
      schema: prod
    permissions:
      - user_name: matthew.giglia@databricks.com
        level: CAN_MANAGE

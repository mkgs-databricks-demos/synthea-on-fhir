# Python dependencies for Databricks Asset Bundles
# Install with: pip install -r requirements.txt

# Databricks Bundles SDK for Python-based resource definitions
databricks-bundles>=0.275.0

# Databricks SDK for workspace API interactions (used in dynamic_deployment.py)
databricks-sdk>=0.30.0

# PyYAML for YAML file parsing and generation
PyYAML>=6.0

resources:
  schemas:
    data_gen_schema:
      name: ${var.schema}
      grants:
        - principal: account users
          privileges:
            - SELECT
            - READ_VOLUME
            - EXECUTE
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.catalog}